servlet:
  multipart:
    max-file-size: 10MB
    max-request-size: 10MB

storage:
  s3:
    endpoint: ${S3_ENDPOINT}
    bucket: moodit
    access-key: ${S3_ACCESS_KEY}
    secret-key: ${s3_SECRET_KEY}

